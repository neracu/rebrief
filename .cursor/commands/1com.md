The module that collects AI instruction files (context harvesting) for REBRIEF.md currently only searches for a flat .cursorrules file in the root of the repository. The search needs to be expanded: recursively check the .cursor/rules/ folder and include all files with the .mdc extension as AI instruction files, on par with .cursorrules, CLAUDE.md, and README.md. Make sure that the found .mdc files are included in the “AI instruction files found” counter in the Project Overview and are used alongside the other rules files wherever they are already applied (e.g., the TODO scan). Add a test for the .cursor/rules/*.mdc case.