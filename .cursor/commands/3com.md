We need to test and improve the detection of hardcoded secrets in the risk scan. Create a test fixture repository (or test files in `tests/fixtures`) with intentionally inserted fake secrets of various types: AWS keys (AKIA...), OpenAI-like keys (sk-...), generic API key/token in environment variables, and a hardcoded password in the config. Run the current regex scanner against these fixtures, use tests to document which patterns are caught and which are not, and expand the regex rules so that all these common secret formats are detected and appear in the CRITICAL section of the Risk Map.